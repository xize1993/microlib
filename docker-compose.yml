version: '3'
services:

#  openjdk:
#    image: openjdk:11-jdk-slim
#    ports:
#      - "8080:8080"
#    volumes:
#      - ./jar:/src
#    working_dir: /src
#    depends_on:
#      - mysql
#    command: java -jar mtmtodo-0.0.1-SNAPSHOT.jar

  mysql:
    image: mysql:5.7
    container_name: microlib-mysql
    ports:
    - "3306:3306"
    volumes:
      - ./dbdata/mysql:/var/lib/mysql
      - ./dbdata/my_cnf:/etc/my.cnf
    environment:
      MYSQL_DATABASE: "microlib"
      MYSQL_ROOT_PASSWORD: "microlib123"
      MYSQL_USER: "dev1"
      MYSQL_PASSWORD: "microlib123"
    restart: always
